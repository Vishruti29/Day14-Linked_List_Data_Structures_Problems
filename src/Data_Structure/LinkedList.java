package Data_Structure;

public class LinkedList {
    public static void main(String[] args) {
            Node node = new Node();
            node.creation(56);
            node.creation(30);
            node.creation(70);
            node.traverse();
        }
    }